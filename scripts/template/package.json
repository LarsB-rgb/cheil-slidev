{
  "name": "@keynotes/{{NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=$((3000 + $(echo $PWD | cksum | cut -d' ' -f1) % 100)) && mkdir -p .claude && echo $PORT > .claude/.dev-port && yes '' 2>/dev/null | slidev slides.md --port $PORT",
    "build": "slidev build",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "catalog:",
    "@slidev/theme-apple-basic": "catalog:",
    "@slidev/theme-default": "catalog:"
  }
}
